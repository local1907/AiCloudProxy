# Screenshots for the AI Cloud Proxy website + README

App screenshots used by the site (`web/index.html`, `#screenshots`) and the repo `README.md`.

| File                | Content                                        | Status |
|---------------------|------------------------------------------------|--------|
| `main-window.png`   | Main window — provider (Gemini), API key, port, Running badge | ✅ added |
| `test-tab.png`      | Test tab — question + streamed answer           | ✅ added |
| `log-options.png`   | Options, feedback panel & live log (active providers) | ✅ added |
| `quick-tour.png`    | First-run Quick Tour dialog                     | ✅ added |
| `system-tray.png`   | System tray context menu (Open / Start-Stop Proxy / Exit) | ✅ added |

The page shows a styled "Screenshot coming soon" placeholder until a file exists, so a
missing file won't break the layout.
